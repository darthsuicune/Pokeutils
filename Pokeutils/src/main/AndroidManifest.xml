<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.suicune.pokeutils"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk android:minSdkVersion="8" android:targetSdkVersion="18" />

    <application android:allowBackup="true"
                 android:name=".App"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light"
        android:icon="@drawable/ic_launcher">
        <activity
            android:name="com.suicune.pokeutils.ui.activities.MainActivity"
            android:theme="@style/Theme.AppCompat.Light"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.suicune.pokeutils.ui.activities.SettingsActivity"
            android:label="@string/settings" >
        </activity>
        <activity
            android:name="com.suicune.pokeutils.ui.activities.EditTeamPokemonActivity"
            android:label="@string/edit_team_pokemon" >
        </activity>

        <provider
            android:name="com.suicune.pokeutils.database.PokeProvider"
            android:authorities="com.suicune.pokeutils.database.PokeProvider"
            android:exported="false" >
        </provider>

    </application>

</manifest>
